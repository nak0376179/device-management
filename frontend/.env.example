# Dev: leave VITE_API_URL unset — Vite proxies /api → VITE_PROXY_TARGET (default http://localhost:8000)
# Prod build: set VITE_API_URL to your deployed API Gateway URL (e.g. https://xxx.execute-api.ap-northeast-1.amazonaws.com)
# VITE_API_URL=
# VITE_PROXY_TARGET=http://localhost:8000
