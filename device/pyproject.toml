[project]
name = "virtual-device"
version = "0.1.0"
description = "Virtual network device that mirrors state via AWS IoT Device Shadow"
requires-python = ">=3.10"
dependencies = [
    "awsiotsdk>=1.21.0",
    "botocore[crt]>=1.20.28",
    "requests>=2.31",
]

[tool.uv]
package = false
